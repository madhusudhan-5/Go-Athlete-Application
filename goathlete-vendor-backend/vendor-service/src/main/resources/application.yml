server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://db.ojymzhgrlrxzhmjyahvp.supabase.co:5432/postgres
    username: postgres
    password: 39syNrPuKF5AD0JZ
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  graphql:
    schema:
      locations: classpath:graphql/
    graphiql:
      enabled: true

logging:
  level:
    org.springframework: INFO
