server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://db.ojymzhgrlrxzhmjyahvp.supabase.co:5432/postgres?sslmode=require
    username: postgres
    password: 39syNrPuKF5AD0JZ
    driver-class-name: org.postgresql.Driver
  security:
    user:
      name: admin
      password: admin
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_schema: go_athlete
    database-platform: org.hibernate.dialect.PostgreSQLDialect
     

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.xml
    drop-first: true
    default-schema: go_athlete

logging:
  level:
    org.springframework: INFO

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
